<% 
const subject = `Invoice Generated — Order #${invoiceNumber}`;
const htmlBody = `
  <p>Hi <strong>${companyName}</strong>,</p>

  <p>Your delivery has been completed and your final invoice has been generated.</p>

  <p>
    Invoice Number: <strong>#${invoiceNumber}</strong><br/>
    Invoice Date: <strong>${new Date(invoiceDate).toLocaleDateString("en-ZA")}</strong><br/>
    Invoice Total: <strong>R${Number(invoiceTotal).toFixed(2)}</strong>
  </p>

  <p>
    You can view or download your invoice here:<br/>
    <a href="${invoiceUrl}" target="_blank">View Invoice</a>
  </p>

  <p>This is an automated message from Bevgo. Please do not reply to this email.</p>
`; 
%>

<%- include("_layout", { subject, body: htmlBody, unsubscribeUrl }) %>
